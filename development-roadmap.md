# Development Roadmap

> **Notion Shared Project Memory is the canonical source of durable project governance.** This document is a working/reference roadmap and must not override canonical Notion records. The one-time controlled governance migration into Notion completed on 2026-09-02 (decisions, ADR-0001–0009, the FR-*/NFR-* requirement set, lifecycle phases/gates, still-relevant risks/open questions, and phase 1–4 approval reviews). `.project-memory/` is the generated one-way mirror of Notion. Where this file and Notion disagree on governance, Notion wins; update Notion, then regenerate the mirror.

## Project

- Objective: Give an individual lifter a calm, local-first mobile app to plan training by week, log workouts fast in the gym offline, and understand progress — without coaching/social/nutrition complexity.
- Application type: Mobile app (workout planner and tracker), single-user personal MVP
- Target platforms: iOS and Android phones (Expo / React Native; Expo Go during prototype/MVP, Expo development build before production)
- Project maturity: Greenfield
- Current lifecycle phase: `platform-release` (AWAITING APPROVAL — executed 2026-09-02; PASS WITH CONDITIONS). **WORK-022 (lifecycle recovery) complete 2026-09-02**: 12 defects surfaced by first local execution (F-1…F-12) all fixed in place; `supabase db reset` ×2 + `db lint` (clean) + `supabase test db` = **PASS 68/68** locally on Postgres 15.8. Phases 6 & 7 stay NOT approved pending the **hosted** re-run (DEC-3: zero hosted-executed tests) + `git`/CI. `client-engineering` stays LOCKED.
- Governance note (human 2026-09-02): the one-time controlled migration into the Notion **Shared Project Memory** is **COMPLETE**. Notion is now the SOLE canonical governance source (governing Decision A); this file + `docs/` are working/reference material and must not override canonical Notion records. Notion Issue "Governance source-of-truth conflict" is Resolved. Future approved governance changes are written to Notion first, then the `.project-memory/` mirror is regenerated.
- Roadmap owner: Human project owner
- Last updated: 2026-09-02

## Lifecycle state

| # | Lifecycle role | Installed skill | State | Result | Human decision |
|---:|---|---|---|---|---|
| 1 | Product strategy | `product-strategy` | APPROVED | PASS WITH CONDITIONS | APPROVED 2026-09-01 (conditions C-1…C-4 accepted as tracked) |
| 2 | UX product design | `evidence-based-ui-ux` | APPROVED | PASS WITH CONDITIONS | APPROVED 2026-09-02 (conditions UX-C1…UX-C4 accepted as tracked; color direction added) |
| 3 | Visual UI design | `visual-ui-design` | APPROVED | PASS WITH CONDITIONS | APPROVED 2026-09-02 (conditions VIS-C1…VIS-C5 accepted as tracked) |
| 4 | Software architecture | `software-architecture` | APPROVED | PASS WITH CONDITIONS | APPROVED 2026-09-02 (v4; conditions AR-C1…AR-C5 accepted as tracked; `backend-data-engineering` unlocked next by explicit human choice) |
| 5 | Client engineering | `client-engineering` | LOCKED | — | Held by human gate: LOCKED until migrations + lint + pgTAP execute successfully on DEP-1 AND `security-identity` is finalised (2026-09-02) |
| 6 | Backend and data engineering | `backend-data-engineering` | APPROVED WITH CONDITIONS | PASS WITH CONDITIONS | APPROVED WITH CONDITIONS 2026-09-02 (BD-C1…BD-C5 accepted; OQ-10 → hard cascade + non-PII receipt; 8 security gates set for phase 7) |
| 7 | Security and identity | `security-identity` | AWAITING APPROVAL | PASS WITH CONDITIONS | Held 2026-09-02: NOT approved as completed — authored + reviewed, but 0 executed tests. Stays `Pass with Conditions` until DEP-1 provisioned and the security verification suite executes successfully. |
| 8 | Platform and release | `platform-release` | AWAITING APPROVAL | PASS WITH CONDITIONS | Executed 2026-09-02 (`docs/platform/platform-release.md`). Local Supabase (Postgres 15.8) provisioned; migrations 0001–0006 + seed apply cleanly and repeatably. First execution surfaced **12 defects** (F-1…F-12; F-5/F-9 High) that inspection missed. **WORK-022 lifecycle recovery (human-authorised) fixed all 12 in place** (migrations unshipped) across `security-identity` (0006: F-2/F-5/F-8/F-11) + `backend-data-engineering` (0003: F-7/F-9) + the pgTAP suites (F-1/F-3/F-4/F-6/F-10/F-12). Re-verified: `supabase db reset` ×2 ✅, `supabase db lint` clean at **warning + error** ✅, `supabase test db` = **PASS 68/68** ✅ (plans 19/17/8/24), runtime probes ✅. SEC-RESID-2 resolved (Edge Function deno.json + deno.lock, `@supabase/supabase-js@2.112.4`). DB-verify CI gate added. **Conditions:** C-1 no git repo (CI can't run; branch protection blocked); C-2 hosted DEP-1 = human decision (all hosted provisioning/auth-hardening/secrets/deploy/PITR deferred); C-3 the gate passes **locally** but on `postgres`-as-owner — phases 6 & 7 stay NOT approved (DEC-3: 0 hosted-executed tests); must re-run green on a provisioned project + CI-gate before `client-engineering`; C-4 the WORK-022 test edits are owned by security/backend (confirm at approval); C-5 dependency pin not-latest; C-6 ISS-27 open (anon read of seed catalogue). |
| 9 | Quality engineering | `quality-engineering` | LOCKED | — | — |
| 10 | Production operations | `production-operations` | LOCKED | — | — |
| 11 | Integrated implementation | `implementation-orchestrator` | LOCKED | — | — |

Allowed states: `LOCKED`, `UNLOCKED`, `IN PROGRESS`, `AWAITING APPROVAL`, `APPROVED`, `APPROVED WITH CONDITIONS`, `BLOCKED`.

## Accepted decisions

| ID | Decision | Owner | Evidence/artifact | Date | Supersedes |
|---|---|---|---|---|---|
| DEC-001 | MVP is single-user and personal; coaching, teams, social, nutrition, wearables, AI programming, video analysis, marketplace, web/desktop authoring, and advanced periodization are out of scope. | `product-strategy` | `docs/product/product-strategy.md` §7.2 | 2026-09-01 | — |
| DEC-002 | Three primary product jobs: (1) log a workout quickly and reliably, (2) plan training by week with reusable items, (3) understand training via history/PRs/progress/trends. | `product-strategy` | `docs/product/product-strategy.md` §4.4 | 2026-09-01 | — |
| DEC-003 | Offline-first reliability is a P0 product invariant: connectivity never blocks logging, completion, or active-session recovery; the offline logging vertical slice is proven before broad screen construction. | `product-strategy` | `docs/product/product-strategy.md` §9, §11.2 | 2026-09-01 | — |
| DEC-004 | Weekly planning is the primary planning model for MVP; performed sets are the source of truth; templates/plans snapshot into sessions and never rewrite completed history. | `product-strategy` | `docs/product/product-strategy.md` §9 | 2026-09-01 | — |
| DEC-005 | Prioritized product requirement set adopted (FR-*/NFR-* with P0–P3) formalizing and validating `SPEC.md` §6; MVP release definition per `docs/product/product-strategy.md` §11.1. | `product-strategy` | `docs/product/product-strategy.md` §8, §11.1 | 2026-09-01 | — |
| DEC-006 | Product IA intent: persistent five-position bottom navigation (Today, Plan, Log(+), Progress, Library); Settings behind an avatar/settings control, not a primary tab. Routed to UX for validation; may be revised with evidence. | `product-strategy` (interaction detail owned by `evidence-based-ui-ux`) | `docs/product/product-strategy.md` §10 CON-11 | 2026-09-01 | Refined by UX-DEC-01 |
| UX-DEC-01 | Five-position bottom nav validated and adopted with one revision: Log (pos 3) is a raised action opening a context sheet, not a navigable destination or back-stack entry; Library retained as a peer tab. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §7.1, §9 | 2026-09-02 | Refines DEC-006 |
| UX-DEC-02 | Settings/preferences/units/export/account behind a top-right avatar control on Today; account-level warnings badge the avatar, not a Today feed item. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §7.2 | 2026-09-02 | — |
| UX-DEC-03 | Active-workout layout = full scrollable session with sticky active-exercise context (Concept B), chosen over single-exercise focus, to support in-session structural edits and fast re-orientation after interruption. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §9.1 | 2026-09-02 | — |
| UX-DEC-04 | Naming: centre action = icon + label "Start or log a workout"; tabs "Today / Plan / Progress / Library"; session end = "Finish" → summary → "Confirm"; local state = "Saved on device" (distinct from "synced"). | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §7.6 | 2026-09-02 | — |
| UX-DEC-05 | Plan tab = week strip + selected-day detail (Concept A), sharing the Today day-strip mental model; expand-all agenda deferred unless usability evidence requires it. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §9.2 | 2026-09-02 | — |
| UX-DEC-06 | "Log a past workout" has the Log sheet as primary entry plus a secondary "Add past workout" action in Progress → History. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §9.3 | 2026-09-02 | — |
| UX-DEC-07 | Set completion = tap-to-complete with inline undo, no per-set confirmation dialog; hold-to-confirm reserved as a defined reversal path for `failure`/`drop` types if evidence requires. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §9.4, §18 UX-RISK-2 | 2026-09-02 | — |
| UX-DEC-08 | Back/recovery: Android back + iOS swipe honoured everywhere; active-workout back minimises (never discards) with a persistent Resume pill; editors autosave locally (no unsaved-changes modal); Finish is idempotent; timer state persisted as timestamps. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §10.6 | 2026-09-02 | — |
| UX-DEC-09 | Experience-intent handoff for visual design: Experience Read, principles UX-P1…P5, six anti-qualities, per-screen attention order, density constraints, 12-surface state matrix, and the constraint that neumorphic elevation / monochrome hue may never be the sole carrier of state. | `evidence-based-ui-ux` | `docs/product/ux-product-design.md` §6, §10, §13, §16 | 2026-09-02 | — |
| VIS-DEC-01 | Platform system: custom cross-platform, iOS-adapted interaction + Android back/insets. Compliance level iOS-inspired / custom — NOT native-HIG compliant; all behaviour re-specified for React Native. | `visual-ui-design` | `docs/design/visual-ui-design.md` §5 | 2026-09-02 | — |
| VIS-DEC-02 | Design direction: primary Utilitarian; supporting Data-dense + Premium-through-restraint. | `visual-ui-design` | `docs/design/visual-ui-design.md` §5 | 2026-09-02 | — |
| VIS-DEC-03 | Palette identity: single Persian-blue tonal scale (accent `#1C39BB`) on a "white sand" warm neutral ground (`#F4F1EA`); dark = "night sand" warm charcoal with the blue lightened. Refines CON-7 to single-hue (not strict greyscale). | `visual-ui-design` | `docs/design/visual-ui-design.md` §5, §6.1; human directive 2026-09-02 | 2026-09-02 | Refines CON-7 |
| VIS-DEC-04 | Signature move: the Set Row — one ruled row (type marker · previous · target · actual load · actual reps · complete), identical across active/past/editor/history; the one choreographed moment is prescribed → active → recorded (140 ms). | `visual-ui-design` | `docs/design/visual-ui-design.md` §5, §7.5 | 2026-09-02 | — |
| VIS-DEC-05 | Restraint rule: only the live session's running elements (active set row, running rest timer, Resume pill) + the Log button may use raised-2 elevation and full-strength blue; ≤1 raised-2 per view; neumorphic shadow is never the sole cue for any state. | `visual-ui-design` | `docs/design/visual-ui-design.md` §5 | 2026-09-02 | — |
| VIS-DEC-06 | No meaning by hue or elevation alone: every state carries ≥2 of {glyph, weight, border-style, shape, text}. Completion = filled marker + check + weight step; PR = triangle/trophy + label (never green); missed day = bar shape; error = danger colour + icon + text. | `visual-ui-design` | `docs/design/visual-ui-design.md` §6.1 | 2026-09-02 | Implements invariant #10 |
| VIS-DEC-07 | Typeface: Aeonik via `expo-font`, used only once exact files/weights/mobile-distribution licence are confirmed (CON-8, DEP-2); documented fallback stack ships meanwhile; no synthesized weights; layout is typeface-independent. 8 type tokens incl. `type.metric`; tabular numerals on all figures. | `visual-ui-design` | `docs/design/visual-ui-design.md` §6.2 | 2026-09-02 | — |
| VIS-DEC-08 | One outlined icon family from the locked Expo Go SDK (1.75–2 px stroke, 20–24 dp, 48 dp hit area, accessible labels); no SF Symbols; final family is a `client-engineering` dependency lock. No imagery/illustration in MVP. | `visual-ui-design` | `docs/design/visual-ui-design.md` §6.5 | 2026-09-02 | — |
| VIS-DEC-09 | `SyncIndicator` is an ambient inline row (never a banner/modal) with provenance wording — "Saved on device" ≠ "synced"; states Saved / Syncing / Offline / Needs attention; may badge the Today avatar. | `visual-ui-design` | `docs/design/visual-ui-design.md` §7.13 | 2026-09-02 | Implements UX-P4 |
| AR-DEC-01 | Local-first: `expo-sqlite` is the on-device system of record; every domain mutation writes row(s) + a `sync_outbox` entry in one transaction; UI never awaits the network. | `software-architecture` | `docs/architecture/adrs/ADR-0001-local-first-system-of-record.md`, `system-architecture.md` §7.2 | 2026-09-02 | — |
| AR-DEC-02 | Enforced layered dependency rule (app → features → domain/services/repository-interfaces → local/sync → remote gateway); Supabase client referenced in one directory; boundary lint is a failing CI check shipped in Phase 0. | `software-architecture` | `docs/architecture/adrs/ADR-0002-layered-dependency-rule.md`, `system-architecture.md` §6 | 2026-09-02 | Implements CON-5 |
| AR-DEC-03 | Sync engine (v4, after three REVISION REQUESTED rounds): transactional outbox with a stable client `operation_id` (server-deduped, exactly-once) and coalesced-latest-state replay. Outbox `state` = `pending` \| **`dispatched`**: `pending` is mutable and holds the partial-unique index (one per `(entity,id)`); **`dispatched` is durable and immutable — removed only by a terminal `applied`/`duplicate`/`conflict`; transport failure / 5xx / timeout / process death keep it `dispatched` and retryable with the same `operation_id`, never returning to `pending` while a `pending` successor exists (so the index is never violated)**; `dispatched` retries before its `pending` successor; return to `pending` only if provably never sent and no successor. **Successor-aware terminal acknowledgement**: a terminated `O1` with a `pending` `O2` re-bases `O2` to the returned `version` and keeps the row `dirty` (never marks synced / never applies `O1`'s payload). **Server-maintained integer `version` optimistic concurrency** — insert-if-absent / update-only-if `base_version = version` / atomic increment / mismatch returns conflict (never overwrite) / same for tombstones. **Hybrid pull:** composite `(updated_at, id)` incremental cursor for latency **plus** periodic full `(id, version)` reconciliation (AR-DEC-11) as the completeness guarantee. Conflict winner decided solely by the `version` protocol, never by device clock. Stale writes preserved in `sync_conflicts`: non-completed auto-reconcile as one fresh `pending` mutation; **completed-session conflicts are parked for explicit user choice and never auto-re-issued (their `O2` is dropped)**. Two-active-session → UI conflict choice, never auto-merge. No CRDT/replication-lib/realtime/server-change-feed in MVP. | `software-architecture` | `docs/architecture/adrs/ADR-0003-sync-engine.md`, `system-architecture.md` §7.2, §8.3–8.4, §10 | 2026-09-02 | Implements CON-3, FR-SYNC-01…05 |
| AR-DEC-11 | Pull completeness guarantee = **periodic full `(id, version)` reconciliation**, not the incremental cursor. `sync_state.last_full_sync` + `Config.fullReconcileIntervalHours` (default 24h) drive it on authenticated cold start / stale foreground / manual sync; per entity it pages a light `(id, version, deleted_at)` projection, fetches full rows only for discrepancies, treats a discrepancy on a `dirty` row as a conflict, and is `updated_at`-independent so it recovers rows whose transaction committed out of `updated_at` order behind the incremental cursor. | `software-architecture` | `docs/architecture/adrs/ADR-0003-sync-engine.md`, `system-architecture.md` §10.3.2 | 2026-09-02 | Closes the late-transaction-commit race |
| AR-DEC-04 | Client-generated UUID PKs (v7 preferred, v4 fallback); UTC instants (epoch-ms / timestamptz, **server-generated `updated_at` by trigger**) + separate IANA session timezone + date-only plan dates; canonical kg/m/s at rest, convert only in presentation; rest timer as an absolute anchor timestamp. | `software-architecture` | `docs/architecture/adrs/ADR-0004-identifiers-and-time.md` | 2026-09-02 | Implements CON-6 |
| AR-DEC-10 | Hot-write acknowledgement (v2): field edits render immediately from component state; the SQLite transaction targets ≤100 ms perceived persistence (SM-4) with no same-frame/≤16 ms commit claim; a set is "recorded" only after commit; on commit failure the values stay editable, the row shows "Not saved — retrying", and Finish is blocked until resolved. | `software-architecture` | `system-architecture.md` §7.2, §11; `docs/architecture/adrs/ADR-0001-local-first-system-of-record.md` | 2026-09-02 | Refines the SM-4 interpretation; aligns with UX §10 persist-failure state |
| AR-DEC-05 | Derived data (PRs, aggregates): deterministic pure recompute over completed performed_sets, materialized locally, idempotent (`recomputeExercise`/`recomputeWeek` delete-and-reinsert in-tx), `formula_id`/`formula_version` stamped; server mirrors the semantics and wins on pull. | `software-architecture` | `docs/architecture/adrs/ADR-0005-derived-data-recompute.md`, `system-architecture.md` §7.3 | 2026-09-02 | Implements FR-DATA-10, NFR-DATA-INTEGRITY |
| AR-DEC-06 | Local SQLite schema mirrors the Postgres schema + 3 local-only tables (`sync_outbox`, `sync_state`, `sync_conflicts`); forward-only numbered migrations with a runner + `schema_migrations`; fresh-create and every upgrade path tested; client/server lockstep via shared entity definitions; FKs RESTRICT for history, CASCADE only for uncommitted children. | `software-architecture` | `docs/architecture/adrs/ADR-0006-local-schema-and-migrations.md`, `system-architecture.md` §8 | 2026-09-02 | — |
| AR-DEC-07 | Client state: React/Context for UI, one small Zustand store for ephemeral session UI only (no domain data); domain data read from SQLite via a thin reactive `useDbQuery`; TanStack Query never used against Supabase. | `software-architecture` | `docs/architecture/adrs/ADR-0007-client-state-and-data-access.md` | 2026-09-02 | — |
| AR-DEC-08 | Runtime schema validation (Zod default; valibot if bundle size warrants) at the `data/remote` gateway (parse every response row) and repository inputs; hand-authored `domain/` types are canonical; generated Supabase types confined to `data/remote`. | `software-architecture` | `docs/architecture/adrs/ADR-0008-boundary-validation.md` | 2026-09-02 | — |
| AR-DEC-09 | Authorization is RLS-enforced server-side only (`user_id = auth.uid()`, per operation); tokens in `expo-secure-store`; per-user local DB file dropped on verified sign-out/deletion; every repo/sync call `userId`-scoped; guest mode deferred behind an `AuthProvider` seam; `delete-account` is the one required Edge Function. | `software-architecture` (policy → `security-identity`) | `docs/architecture/adrs/ADR-0009-authorization-and-identity-posture.md`, `system-architecture.md` §5, §9 | 2026-09-02 | Implements CON-4, FR-AUTH-05 |
| BD-DEC-01 | Server schema: all SPEC §9 entities with the standard sync columns; `user_id` denormalised onto every table (incl. children) for join-free RLS; server-authoritative `created_at`/`updated_at`/`version` via one `set_row_metadata()` BEFORE trigger (client values ignored; `version` = 1 on insert, `OLD.version+1` on every update incl. tombstone). Template content versioning renamed `content_version` (collides with the sync `version`). Five forward-only migrations (`20260902090001..05`). | `backend-data-engineering` | `docs/engineering/backend-data-implementation.md` §4–5; `supabase/migrations/` | 2026-09-02 | Implements AR-DEC-04/06, SPEC §9 |
| BD-DEC-02 | `sync_apply(operation_id, entity, entity_id, op, payload, base_version) → jsonb` RPC (`SECURITY INVOKER`) is the single push entry point: `operation_id` dedupe via `processed_operations` (records only `applied`/`duplicate`; `conflict` not recorded so a retry re-evaluates); insert-if-absent → v1; update/tombstone only if `base_version = version`; mismatch → `{status:conflict, version, row}` with nothing written. RLS scoping makes cross-account `entity_id` behave as a rejected insert. AR-OQ-6 resolved toward the RPC; conditional-PATCH remains a valid alternative (trigger owns the bump). | `backend-data-engineering` | `docs/engineering/backend-data-implementation.md` §6.1; `supabase/migrations/20260902090004_sync_apply.sql` | 2026-09-02 | Implements AR-DEC-03, §8.4 invariant |
| BD-DEC-03 | Server recompute (AR-OQ-3 → trigger-driven): `recompute_exercise_prs` / `recompute_session_volume_pr` / `recompute_week_aggregates` are pure, deterministic (ordering key `(session_exercise_id, position, id)`), idempotent (deterministic `uuid_generate_v5` derived-row ids → pure UPSERT + tombstone-the-rest), `formula_id`/`formula_version` stamped; Epley e1RM for reps 2–10; warmups excluded from headline volume. AFTER triggers on `performed_sets` + `workout_sessions` fire when the parent session is completed. Golden vectors shared with the client TS impl (WORK-012). | `backend-data-engineering` | `docs/engineering/backend-data-implementation.md` §7; `supabase/migrations/20260902090003_recompute.sql`, `supabase/tests/03_recompute_test.sql` | 2026-09-02 | Implements AR-DEC-05, SPEC §12 |
| BD-DEC-04 | RLS baseline: `enable` + `force` RLS on every table; per-command policies (`user_id = auth.uid()`; `exercises` special-cases global seed rows; `profiles` keys on `id`); derived tables read-only to clients; `processed_operations` append-only owner-scoped. | `backend-data-engineering` (baseline) → `security-identity` (final) | `supabase/migrations/20260902090005_rls.sql` | 2026-09-02 | Superseded/owned by SEC-DEC-01…05 |
| BD-DEC-05 | `delete-account` Edge Function skeleton. | `backend-data-engineering` | `supabase/functions/delete-account/index.ts` | 2026-09-02 | Finalised by SEC-DEC-04 |
| SEC-DEC-01 | Authorization boundary = RLS + `SECURITY INVOKER` functions + FK integrity, all server-side; client assumed hostile. Every user-owned table: per-command policies, predicate `user_id = auth.uid()`. `security-identity` owns `supabase/migrations/20260902090005` + `20260902090006`; any later change to a policy / ownership FK / definer function / `sync_apply` routes back here. | `security-identity` | `docs/security/security-identity.md` §3, §5; `supabase/migrations/20260902090006_security_hardening.sql` | 2026-09-02 | Implements ADR-0009, NFR-SEC |
| SEC-DEC-02 | Child→parent ownership integrity: composite `(parent_id, user_id) → parent(id, user_id)` FKs on the 8 structural relationships + `_check_ref_ownership()` trigger for soft references (`exercise_id`, `workout_template_id`, …). A child's `user_id` can only reference a same-owner parent; no cross-tenant parent linkage or id-existence oracle. | `security-identity` | `docs/security/security-identity.md` §6 SEC-F-1; `supabase/migrations/20260902090006` S-1 | 2026-09-02 | Resolves phase-6 gate 2; fixes SEC-F-1 (High) |
| SEC-DEC-03 | Derived-table write model (resolves BD-OQ-2 / gate 3): derived tables `ENABLE` (not `FORCE`) RLS for owner-scoped reads, **all client DML revoked**; `recompute_*` + recompute triggers are `SECURITY DEFINER` (owner bypasses non-FORCE RLS) with fixed `search_path = pg_catalog, public`, `REVOKE ALL … FROM public`, no `authenticated` execute grant (trigger-only). `sync_apply` hardened: fixed `search_path`, per-entity ownership column forced to caller, `owner_user_id` stripped, FK/constraint errors normalised to a generic `{status:"rejected"}`. | `security-identity` | `docs/security/security-identity.md` §5 SEC-REQ-AZ-04/05/07, §6 SEC-F-2/4/5; `supabase/migrations/20260902090006` S-2/S-8 | 2026-09-02 | Resolves BD-OQ-2, gates 3+5; fixes SEC-F-2/4/5 |
| SEC-DEC-04 | Account deletion (OQ-10): **hard cascade** — `user_id → auth.users(id) ON DELETE CASCADE` added to `personal_records`/`weekly_aggregates`/`exercise_weekly_rollups`/`processed_operations` so deleting the auth user removes the whole graph. Retain only `deletion_receipts` (keyed HMAC of the user id + app version + timestamps; `FORCE` RLS, no policies → service_role-only) OUTSIDE the user graph. `delete-account` rewritten: re-auth = `iat` + `last_sign_in_at` within 300 s; receipt written before the destructive call. | `security-identity` | `docs/security/security-identity.md` §5 SEC-REQ-DATA-01/02, §6 SEC-F-3/6; `supabase/migrations/20260902090006` S-3 + `deletion_receipts`; `supabase/functions/delete-account/index.ts` | 2026-09-02 | Resolves OQ-10, gate 7; fixes SEC-F-3/6 |
| SEC-DEC-05 | Global-seed `exercises` is an accepted dual-tenancy exception to the `user_id NOT NULL` contract: `owner_user_id IS NULL` = global (migrations / `service_role` only), `= auth.uid()` = private. Guarded by RLS + `_guard_exercise_owner()` trigger (blocks client creation of a global row and any re-parenting). | `security-identity` | `docs/security/security-identity.md` §5 SEC-REQ-AZ-06, §6 SEC-F-7; `supabase/migrations/20260902090006` S-4 | 2026-09-02 | Resolves gate 4; documents the deviation |

## Constraints

| ID | Constraint | Source | Affected phases | Status |
|---|---|---|---|---|
| CON-1 | Client is React Native + Expo + Expo Router + strict TypeScript. | `CLAUDE.md`, `SPEC.md` §10.1 | Architecture, Client | ACTIVE |
| CON-2 | Expo Go compatibility through prototype/MVP; versions via `expo install` and locked; no custom native modules until an approved dev-build migration. | `CLAUDE.md`, `SPEC.md` §10.2 | Architecture, Client, Platform | ACTIVE |
| CON-3 | `expo-sqlite` is the local operational DB and normal read/write source; domain mutations persisted transactionally with an outbox entry. | `CLAUDE.md`, `SPEC.md` §11 | Architecture, Backend, Client | ACTIVE |
| CON-4 | Supabase for auth + synchronized Postgres; RLS enabled and adversarially tested before exposing any user-owned table; service-role key never shipped/requested. | `CLAUDE.md`, `SPEC.md` §10.3 | Architecture, Backend, Security | ACTIVE |
| CON-5 | UI components must not call Supabase directly; access flows through feature logic → domain services → repository interfaces → local repositories → sync → Supabase gateway. | `CLAUDE.md`, `SPEC.md` §10.4 | Architecture, Client, Backend | ACTIVE |
| CON-6 | Instants stored in UTC with session timezone stored separately; plan dates are date-only. | `CLAUDE.md` | Architecture, Backend | ACTIVE |
| CON-7 | Visual system is single-hue (not strict greyscale), softly neumorphic, utilitarian, data-aware, restrained; custom cross-platform with iOS-adapted interaction, not a native-iOS-compliance claim. Human directive 2026-09-02: "white sand" warm off-white ground + "Persian blue" tonal scale as the single hue. | `CLAUDE.md`, `SPEC.md` §8, human 2026-09-02 | Visual UI, UX | ACTIVE (refined) |
| CON-8 | Aeonik used only once exact files, weights, and mobile distribution license are available; missing weights never synthesized. | `CLAUDE.md`, `SPEC.md` §8.4 | Visual UI, Platform | ACTIVE |
| CON-9 | Analytics behind an interface; credentials, private notes, workout payloads, and sensitive personal values excluded from production telemetry. | `CLAUDE.md`, `SPEC.md` §16 | Architecture, Security, Operations | ACTIVE |
| CON-10 | MVP is single-user and personal; MVP scope not expanded without an owned decision and human approval. | `CLAUDE.md` | All phases | ACTIVE |
| CON-11 | Product IA intent: five-position bottom nav; Settings not a primary tab. Interaction design owned by `evidence-based-ui-ux`. | `SPEC.md` §4, `docs/product/product-strategy.md` §10 | UX | ACTIVE |

## Dependencies and external capabilities

| ID | Need | Type | Required by | Status | Human action |
|---|---|---|---|---|---|
| DEP-1 | Supabase project (Auth + Postgres + RLS), local + hosted | Infrastructure / service | Architecture, Backend | NOT PROVISIONED | Create a Supabase project; provide publishable/anon config only (never the service-role key). |
| DEP-2 | Aeonik font files + mobile app distribution license | Licensed asset | Visual UI, Platform | NOT AVAILABLE | Procure licensed files; record license proof (OQ-2). |
| DEP-3 | Legally usable seed exercise catalog + content license | Licensed data | Backend (Library) | NOT IDENTIFIED | Select and clear a dataset license (OQ-4). |
| DEP-4 | Analytics / crash-reporting provider (Expo Go-compatible) | Service / SDK | Operations, Security | NOT CHOSEN | Choose provider and consent model (OQ-9). |
| DEP-5 | EAS build / distribution (dev build, app signing, store accounts) | Infrastructure / accounts | Platform | NOT SET UP | Set up EAS and store accounts before production hardening. |

Never request credentials in this file. Record only the capability, authorization state, and next safe action.

## Open questions

| ID | Question | Why it matters | Owner | Blocking? | Resolution |
|---|---|---|---|---|---|
| OQ-1 | Final product name, icon, and brand wordmark. | Store listing, app identity, visual system | Human | NO | — |
| OQ-2 | Exact Aeonik files/weights + proof of mobile app distribution license. | Typography cannot be finalized/shipped without it | Human | NO | — |
| OQ-3 | Ship guest mode? Only if guest-to-account migration is atomic and lossless (FR-AUTH-04). | Onboarding funnel vs. data-loss risk and build cost | Human + Product | NO | — |
| OQ-4 | Exact seeded exercise catalog and its content license. | FR-LIB-03 cannot ship without a legally usable dataset | Human | NO | — |
| OQ-5 | Do bodyweight/assisted exercises include body mass in progress calculations? | Correctness of e1RM/volume/PRs for a large exercise class | Product | NO | — |
| OQ-6 | Allow alternative e1RM formulas after MVP? | Whether formula-id versioning must support user choice | Product | NO | — |
| OQ-7 | Sync conflict UX for true multi-device simultaneous editing. | MVP rule is last-accepted-write-wins + telemetry; richer UX may be needed | UX + Architecture | NO | — |
| OQ-8 | Is dark mode a launch requirement or post-MVP polish? | Tokens support it regardless; state-coverage scope differs | Product + Visual UI | NO | — |
| OQ-9 | Analytics/crash provider and privacy consent requirements. | Consent flow, telemetry schema, store disclosures | Human + Security | NO | — |
| OQ-10 | Data retention period and completed-account-deletion server behavior (cascade vs. anonymize). | Legal/privacy posture; FR-SET-03 and NFR-PRIVACY completion | Human + Security | NO | **RESOLVED 2026-09-02: hard cascade deletion for MVP; retain only a non-PII deletion receipt outside the deleted user graph.** Retention period for other data still TBD → tracked as SEC-OQ. |
| UX-OQ-1 | Measured baselines for SM-1…SM-7 and whether the targets are realistic. | Blocks treating SM-* as release gates | `evidence-based-ui-ux` + `quality-engineering` | NO (not for visual design) | — |
| UX-OQ-2 | Should completed exercises in a long active session collapse by default? | Legibility/re-orientation vs. scroll length in long sessions | `evidence-based-ui-ux` | NO | Depends on WORK-002 |
| UX-OQ-3 | Does copy+adjust a week meet SM-6 (≤3 actions / <2 min), or is an expand-all agenda mode needed? | Planning friction target | `evidence-based-ui-ux` | NO | Depends on WORK-002 |
| UX-OQ-4 | Onboarding depth: full SPEC §6.1 set vs. unit-only blocking with the rest deferred to first use. | First-run friction; interacts with guest mode (OQ-3) | `evidence-based-ui-ux` + Product | NO | — |
| UX-OQ-5 | Dark mode at launch vs. token-readiness only (UX recommends launch; Product's call). | Gym lighting varies; scope of state coverage | Product + `visual-ui-design` | NO | Same as OQ-8 |
| UX-OQ-6 | Exact copy/options for the multi-device active-session conflict choice (FR-SYNC-05). | Needs design once sync model is decided | `evidence-based-ui-ux` + `software-architecture` | NO | Same as OQ-7 |
| VIS-OQ-1 | Dark mode at launch vs. token-readiness only (full light+dark tokens delivered; only 2 screens rendered dark). | Scope of dark renders + QA | Product + `visual-ui-design` | NO | Same as OQ-8 / UX-OQ-5 |
| VIS-OQ-2 | Exact Aeonik files/weights + mobile app-distribution licence (scale assumes Regular/Medium/Semibold/Bold). | Typography cannot be finalized/shipped without it | Human | NO (fallback ships) | Same as OQ-2 / DEP-2 |
| VIS-OQ-3 | Final outlined icon family available in the locked Expo Go SDK. | Icon dependency lock | `client-engineering` | NO | WORK-008 |
| VIS-OQ-4 | Is `#9B2C2C` danger the only non-blue hue, or may chart/warning states use an amber tier? | Mono chart readability | `visual-ui-design` | NO | Revisit with UX-RISK-3 evidence |
| VIS-OQ-5 | On-device: do the softened neumorphic pairs read in gym light, or drop elevation further toward flat + border? | Legibility vs. depth | `visual-ui-design` + `quality-engineering` | NO | WORK-002 on-device session / WORK-007 |
| AR-OQ-1 | UUIDv7 generator availability/quality in the locked Expo Go SDK (else v4). | Index locality vs. availability | `software-architecture` + `client-engineering` | NO | v4 is a safe default |
| AR-OQ-2 | Validation library: Zod vs valibot (bundle size vs ergonomics) on RN. | Bundle size | `client-engineering` | NO | — |
| AR-OQ-3 | Server `recompute` as a SQL function/trigger vs an Edge Function (transactional simplicity vs byte-identical to TS). | Drift risk vs. transactionality | `backend-data-engineering` | NO | Both satisfy §10.5 |
| AR-OQ-4 | Hand-rolled `useDbQuery` vs adopting an Expo Go-compatible reactive SQLite layer from the start. | Perf/invalidation risk vs. Expo Go boundary | `software-architecture` + `client-engineering` | NO | AR-RISK-3; WORK-010 |
| AR-OQ-5 | Multi-device concurrent-edit **review UX** — how `sync_conflicts` are surfaced/resolved by the user (the `version` protocol itself is fixed; no data-loss question). | UX under heavy multi-device use | `software-architecture` + `evidence-based-ui-ux` | NO | Same as OQ-7 |
| AR-OQ-6 | Concurrency mechanism: one `sync_apply` `rpc` function vs conditional PostgREST PATCH + separate `processed_operations` upsert. | Atomicity / round-trips | `backend-data-engineering` | NO | RESOLVED toward the RPC (BD-DEC-02); PATCH remains valid |
| BD-OQ-1 | Weekly-aggregate bucketing vs. the user's configurable `week_start` (SET-01). | `weekly_aggregates` semantics | `backend-data-engineering` + `client-engineering` | NO | **Human direction 2026-09-02: use the user's configured `week_start` AND the session-local calendar date (not UTC `date_trunc`).** To implement before the Data & Progress increment. |
| BD-OQ-2 | Exact write-grant / definer model for the recompute triggers under FORCE RLS on the derived tables. | Recompute can't write derived rows otherwise | `security-identity` | NO | **Human direction 2026-09-02: narrowly scoped recompute functions, safe ownership, fixed `search_path`, explicit execute grants under FORCE RLS.** Addressed in phase 7. |
| BD-OQ-3 | Should `sync_apply` accept a batch of ops to cut round-trips, or stay one-op-per-call (current). | Sync throughput | `backend-data-engineering` | NO | Later optimisation |
| BD-OQ-4 | `processed_operations` retention / pruning policy (unbounded append). | Table growth | `production-operations` + `backend-data-engineering` | NO | — |
| SEC-OQ-1 | Data retention policy for non-deleted data + backups / PITR window (OQ-10 covered deletion mechanics only). | Beta privacy statement | Human + `production-operations` | NO | Blocks beta, not MVP build |
| SEC-RESID-1 | `delete-account` re-auth is a 300 s freshness heuristic, not nonce-based reauthentication. | Irreversible action gating | `security-identity` + `client-engineering` | NO | Human 2026-09-02: development-only; **server-verifiable re-auth required before beta** (not GA) |
| SEC-RESID-2 | Edge Function imports `@supabase/supabase-js@2` from `esm.sh` unpinned. | Supply chain | `platform-release` + `quality-engineering` | NO | Must fix before first hosted deploy |

## Known risks

| ID | Risk | Likelihood | Impact | Mitigation owner | Status |
|---|---|---|---|---|---|
| RISK-1 | Offline sync + snapshot-vs-reference integrity underestimated, threatening the core invariant. | Medium | High | Architecture, Backend | OPEN |
| RISK-2 | No user research; user model and success targets SM-1…SM-7 are assumptions. | High | Medium | UX, Quality | OPEN |
| RISK-3 | Aeonik license/files unavailable at release (OQ-2). | Medium | Medium | Visual UI, Platform | OPEN |
| RISK-4 | Seed exercise catalog licensing (OQ-4) delays or shrinks FR-LIB-03. | Medium | Medium | Product, Backend | OPEN |
| RISK-5 | Expo Go constraints force compromises that do not survive the dev-build migration. | Medium | Medium | Architecture, Platform | OPEN |
| RISK-6 | Neumorphic visual direction weakens accessibility (state conveyed by elevation/color). | Medium | High | Visual UI, UX, Quality | OPEN |
| RISK-7 | RLS misconfiguration exposes cross-account data. | Low | High | Security, Backend, Quality | OPEN |
| UX-RISK-1 | UX validated only by description + an analogical reference; real-device gym use may invalidate reachability/keyboard/interruption assumptions. | High | Medium | `evidence-based-ui-ux`, Quality | OPEN |
| UX-RISK-2 | One-tap set completion without confirmation (UX-DEC-07) causes accidental completions undo doesn't fully catch. | Medium | Medium | `evidence-based-ui-ux` | OPEN |
| UX-RISK-3 | Monochrome + neumorphic direction makes completion/PR/selection/error hard to distinguish, degrading the logging read. | Medium | High | `visual-ui-design`, Quality | OPEN |
| UX-RISK-4 | "Saved on device" vs "synced" misunderstood — users over-trust local durability or worry unnecessarily. | Medium | Medium | `evidence-based-ui-ux` | OPEN |
| UX-RISK-5 | Progress Overview headline metrics ambiguous/misleading with sparse data. | Medium | Medium | `evidence-based-ui-ux`, Backend | OPEN |
| UX-RISK-6 | Description-based state matrix (§10) misses states that only emerge in implementation (e.g. partial session sync). | Medium | Medium | Client, Quality | OPEN |
| VIS-RISK-1 | Persian blue `#1C39BB` is dark/saturated; large blue fills may feel heavy or vibrate against warm sand. | Medium | Medium | `visual-ui-design` | OPEN |
| VIS-RISK-2 | Monochrome + neumorphic system makes completion/PR/selection/error hard to distinguish (carried from UX-RISK-3). | Medium | High | `visual-ui-design`, Quality | OPEN |
| VIS-RISK-3 | "Ledger" flatness reads as austere/unfinished to some users. | Medium | Medium | `visual-ui-design` | OPEN |
| VIS-RISK-4 | Android cannot render the dual neumorphic pair; platforms diverge visually (documented fallback exists). | High | Low | `client-engineering` | OPEN |
| VIS-RISK-5 | Aeonik never licensed; fallback stack shifts perceived character. | Medium | Medium | `platform-release`, `visual-ui-design` | OPEN |
| VIS-RISK-6 | Visual system validated in a browser prototype, not on device (Dynamic Type reflow, VoiceOver order, RTL, real shadows unverified). | High | Medium | `client-engineering`, Quality | OPEN |
| AR-RISK-1 | The `version` protocol prevents lost writes, but heavy multi-device concurrent use could generate a frustrating volume of `sync_conflicts` against a thin MVP review UX. | Medium | Medium | `software-architecture`, `evidence-based-ui-ux` | OPEN |
| AR-RISK-2 | Client (TS) and server (SQL/Edge) `recompute` implementations drift → different PRs/aggregates across devices. | Medium | High | `software-architecture`, `backend-data-engineering`, Quality | OPEN |
| AR-RISK-3 | Hand-rolled reactive query layer over `expo-sqlite` is inadequate; adopting a replication lib later breaks Expo Go and forces an early dev-build. | Medium | Medium | `software-architecture`, `client-engineering` | OPEN |
| AR-RISK-4 | `expo-sqlite` transactional/WAL guarantees in the locked SDK are weaker than assumed → outbox atomicity at risk. | Low | High | `software-architecture`, `client-engineering` | OPEN |
| AR-RISK-5 | Snapshot duplication (template→planned→session) implemented inconsistently → mutable template data leaks into history. | Medium | High | `software-architecture`, `client-engineering`, Quality | OPEN |
| AR-RISK-6 | Boundary-lint not wired to CI → layering erodes, silently breaking the offline guarantee. | Medium | High | `client-engineering`, Quality | OPEN |
| AR-RISK-7 | Sync protocol implemented incorrectly: cursor advanced to `max(updated_at)`; non-atomic version check+increment; `operation_id` not persisted pre-request; terminal ack clears `dirty` / applies `O1`'s payload while a `pending` `O2` exists; a transport-failed `dispatched` op returned to `pending` (index violation) or terminated non-terminally; completed-session conflict auto-re-issued instead of parked. → reintroduces lost updates, skipped rows, or index violations. | Medium | High | `software-architecture`, `backend-data-engineering`, Quality | OPEN |
| AR-RISK-8 | Full reconciliation (§10.3.2) never runs (bug / `fullReconcileIntervalHours` too high / app never cold-started) → a late-committed or cursor-skipped server row is missed for a long time. | Low | Medium | `software-architecture`, `production-operations` | OPEN |
| BD-RISK-1 | `supabase/` migrations/functions/tests authored but never executed — a syntax/semantic error is undiscovered. | Medium | High | `backend-data-engineering`, Quality, Platform | **REALIZED then REMEDIATED 2026-09-02.** First local execution found 12 defects (F-1…F-12; F-5 `_week_start_for(date,integer)`, F-9 `uuid_ns_url` unresolved via pinned search_path — both would break the recompute path at runtime; F-2 `sync_apply` NOT NULL leak; F-8 dedupe status; F-11 anon EXECUTE; + 6 test defects). All fixed under WORK-022; `db reset` + `db lint` + `supabase test db` (68/68) + runtime probes green on Postgres 15.8. **Residual (OPEN):** hosted role model (`service_role` BYPASSRLS, GoTrue) unverified — re-run on DEP-1 + CI-gate. |
| BD-RISK-2 | `recompute_week_aggregates` buckets by `date_trunc('week')` (Mon) but the product supports a configurable `week_start` → weekly aggregates may not match the user's week. | Medium | Medium | `backend-data-engineering`, `client-engineering` | OPEN (BD-OQ-1) |
| BD-RISK-3 | Trigger-driven recompute on every `performed_sets` write could become hot under a future bulk import. | Low | Medium | `backend-data-engineering` | OPEN |
| BD-RISK-4 | Recompute triggers need write rights on FORCE-RLS derived tables with no write policy — grant/definer model unconfirmed. | Medium | Medium | `security-identity`, `backend-data-engineering` | RESOLVED by SEC-DEC-03 (untested — SEC-C1) |
| BD-RISK-5 | `delete-account` defaults to hard cascade; if OQ-10 → anonymise, the FK design and the function change. | Medium | Medium | Human, `security-identity` | RESOLVED — OQ-10 = hard cascade (SEC-DEC-04) |
| SEC-RISK-1 | All phase-7 controls authored but **not executed** — a runtime error is undiscovered. | Medium | High | `platform-release`, `quality-engineering` | **LOCAL EXECUTION GREEN 2026-09-02** after WORK-022: all four pgTAP suites pass (68/68) on Postgres 15.8 — cross-account isolation, forged-`user_id`/cross-tenant-parent rejection, derived-table write revocation, `anon` cannot call `sync_apply` (F-11 fixed), seed immutable-to-clients, null-`sub` JWT sees nothing, generic reject (no oracle). **OPEN (SEC-C1):** re-run on the hosted role model + CI-gate before `client-engineering`. |
| SEC-RISK-2 | Trigger-heavy write path (`set_row_metadata` + `_check_ref_ownership` + `_guard_exercise_owner` + recompute) — a bug in any BEFORE trigger blocks legitimate writes. | Medium | Medium | `quality-engineering` | **EXERCISED 2026-09-02** (local): F-5 (recompute trigger) + F-9 (recompute helper) found & fixed; full write path + all triggers green under `tests/01`–`04` + runtime probes. Re-verify hosted. |
| BD-RISK-6 | `seed.sql` is a non-shippable placeholder; shipping it is a licensing problem. | Low | Medium | Human, `backend-data-engineering` | OPEN (OQ-4) |
| BD-RISK-7 | Client SQLite mirror and server schema drift over time (no shared machine-readable entity definition yet). | Medium | Medium | `client-engineering`, `backend-data-engineering` | OPEN |

## Work ledger

| ID | Work item | Owning phase | Status | Evidence | Deferred reason |
|---|---|---|---|---|---|
| WORK-001 | Product strategy contract: users, jobs, value, scope, prioritized requirements, success measures, constraints, risks, release intent. | `product-strategy` | COMPLETE | `docs/product/product-strategy.md` | — |
| WORK-002 | Establish usability-test baselines for SM-1…SM-7 (tap counts, timings) via the §14 research plan, incl. one moderated on-device gym-context session. | `evidence-based-ui-ux` + `quality-engineering` | PLANNED | `docs/product/ux-product-design.md` §14 | Requires runnable build + participants |
| WORK-003 | Validate/refine product IA (CON-11 five-position nav) against evidence. | `evidence-based-ui-ux` | COMPLETE | `docs/product/ux-product-design.md` §7.1, §9 (UX-DEC-01) | — |
| WORK-004 | UX product design: IA, six primary journeys, concept comparisons, friction-reduction interaction decisions, 12-surface state matrix, accessibility targets, visual-design handoff. | `evidence-based-ui-ux` | COMPLETE | `docs/product/ux-product-design.md` | — |
| WORK-005 | Resolve UX open questions UX-OQ-2/3 (active-session collapse; week copy meets SM-6) using WORK-002 evidence. | `evidence-based-ui-ux` | PLANNED | `docs/product/ux-product-design.md` §17 | Depends on WORK-002 |
| WORK-006 | Visual system v1: direction, tokens (light+dark), typography, 16 components with states, screen application, rendered prototype + critique pass, Design Memory. | `visual-ui-design` | COMPLETE | `docs/design/visual-ui-design.md`, `docs/design/renders/weight-visual-system-v1.html` | — |
| WORK-007 | On-device verification of the visual system: Dynamic Type reflow of Set Row/ledger, VoiceOver/TalkBack order, RTL mirroring, real neumorphic rendering, measured contrast, Persian-blue weight on sand. | `client-engineering` + `quality-engineering` | PLANNED | `docs/design/visual-ui-design.md` §9 (VIS-AX-01…10) | Requires runnable build |
| WORK-008 | Lock the outlined icon family within the chosen Expo Go SDK (`expo install`, exact version). | `client-engineering` | PLANNED | `docs/design/visual-ui-design.md` §6.5 (VIS-DEC-08) | Depends on SDK selection in architecture |
| WORK-009 | System architecture: context/trust boundaries, layered decomposition + enforcement, runtime flows, domain model, sync/consistency model, quality attributes, 9 ADRs, traceability. | `software-architecture` | COMPLETE | `docs/architecture/system-architecture.md`, `docs/architecture/adrs/` | — |
| WORK-010 | Verify locked Expo SDK: `expo-sqlite` transaction/WAL/prepared-statement guarantees + feasibility of a hand-rolled reactive query layer; if inadequate, pull the dev-build migration earlier. | `software-architecture` + `client-engineering` | PLANNED | `system-architecture.md` §4.2 (AR-A4), §17.1 (AR-RISK-3/4) | Phase 0, before building the write path |
| WORK-011 | Ship the boundary-lint config (`dependency-cruiser`/eslint-boundaries) + a network-stubbed logging-flow smoke test as a failing CI gate. | `client-engineering` + `quality-engineering` | PLANNED | `docs/architecture/adrs/ADR-0002-layered-dependency-rule.md` | Phase 0 |
| WORK-012 | Shared golden test vectors for `recompute` run against both the TS `domain` implementation and the server SQL/Edge implementation. | `backend-data-engineering` + `quality-engineering` | IN PROGRESS | server side authored: `supabase/tests/03_recompute_test.sql`; client TS side + cross-run pending | Depends on DEP-1; client impl |
| WORK-014 | Backend & data layer: schema, 5 forward-only migrations, `sync_apply` RPC, deterministic recompute + triggers, RLS baseline, `delete-account` skeleton, 3 pgTAP suites. | `backend-data-engineering` | COMPLETE (authored, not executed — DEP-1) | `docs/engineering/backend-data-implementation.md`, `supabase/` | 2026-09-02 |
| WORK-015 | Provision DEP-1, apply migrations + seed, run `supabase test db` + `supabase db lint`; wire as a CI merge gate. | `platform-release` + `quality-engineering` | IN PROGRESS | `docs/platform/platform-release.md`, `docs/platform/evidence/`, `.github/workflows/db-verify.yml` | **Local** done 2026-09-02: `db reset` ✅, `db lint --fail-on error` ✅, `db-verify.yml` gate created. `db test` ❌ (F-2/F-5). Hosted apply still needs DEP-1. |
| WORK-016 | `security-identity`: ownership of RLS migrations; child→parent composite-FK integrity; BD-OQ-2 (definer recompute); global-seed guard; hardened `sync_apply`; OQ-10 hard cascade + non-PII receipt; `delete-account` rewrite; 24-assertion adversarial suite; threat model. | `security-identity` | COMPLETE (authored, not executed — DEP-1) | `docs/security/security-identity.md`, `supabase/migrations/20260902090006`, `supabase/tests/04`, `supabase/functions/delete-account/index.ts` | 2026-09-02 |
| WORK-018 | Execute `supabase db reset` (migrations 0001–0006 + seed) + `supabase test db` (pgTAP suites 01–04) green; wire as the CI merge gate. **Blocks `client-engineering`.** | `platform-release` + `quality-engineering` | IN PROGRESS | `.github/workflows/db-verify.yml`, `docs/platform/evidence/03-supabase-test-db.txt` | Gate created + executed locally 2026-09-02. `db reset` green; **`db test` RED** — blocked by F-5 (High) + F-2 (Med), routed to `security-identity`/`backend-data-engineering`. `client-engineering` stays LOCKED until green. |
| WORK-019 | Hosted auth hardening: email confirmation, user-enumeration protection, leaked-password protection, auth rate limits; provision `SUPABASE_SERVICE_ROLE_KEY` + `DELETION_RECEIPT_HMAC_KEY`; pin the Edge Function dependency + Deno lockfile. | `platform-release` | IN PROGRESS | `docs/platform/platform-release.md` §8–9; `supabase/config.toml`; `supabase/functions/delete-account/deno.json` + `deno.lock` | **Dependency pin + Deno lockfile DONE** 2026-09-02 (SEC-RESID-2 resolved; `@supabase/supabase-js@2.112.4`). Hosted auth hardening intent captured in `config.toml` + artifact §8; **application deferred to DEP-1**. |
| WORK-022 | Lifecycle recovery — fix the defects surfaced by phase-8 first execution. **COMPLETE 2026-09-02.** Migration fixes: F-5 (`1::smallint` at 4 `_week_start_for` boundaries; signature preserved), F-2 (`not_null_violation` → structured `{status:"rejected"}`), F-8 (dedupe returns `duplicate` per contract), F-9 (schema-qualify `extensions.uuid_*` in `_pr_id`/`_agg_id`), F-11 (`revoke sync_apply from anon`), F-7 (`array[]::uuid[]`). Test fixes: F-1 (`perform`→`select`, ratified), F-3/F-4 (0-rows + unchanged assertions), F-6 (plans → 19/17/8/24), F-10 (anon assertion → "no private exercises"; ISS-27 opened), F-12 (`::numeric` casts), full-row payloads + partial-payload negative test. Verified: `db reset` ×2 ✅, `db lint` clean ✅, `supabase test db` **PASS 68/68** ✅, runtime probes ✅. Evidence: `docs/platform/evidence/05-08`. | `security-identity` + `backend-data-engineering` | AWAITING APPROVAL | `docs/security/security-identity.md` §8.1, `docs/engineering/backend-data-implementation.md` §9.1, `docs/platform/platform-release.md` §10.1/§13 | Local gate met; **hosted re-run + CI gate + client↔server golden-vector cross-run still required** before `client-engineering` unlock and phase 6/7 approval. |
| WORK-020 | Backend validates SEC-F-9 (corrected weekly bucketing: user `week_start` + session-local date) against the WORK-012 golden vectors. | `backend-data-engineering` | IN PROGRESS | `docs/engineering/backend-data-implementation.md` §9.1 | WORK-022 fixed the F-5 resolution bug in this code and probed `week_start` 0–6 + `tests/03` vectors (weekly volume 1430, e1RM 116.6667/129.8333) green locally. **Formal client TS ↔ server cross-run still owed** before Data & Progress. |
| WORK-021 | Pre-beta: nonce-based re-auth for `delete-account` (SEC-RESID-1); data-retention/backup policy (SEC-OQ-1); external penetration test (SEC-RESID-6). | `security-identity` + Human | PLANNED | `docs/security/security-identity.md` §9, §11 | Before beta |
| WORK-017 | Extract a shared machine-readable entity definition as the client↔server schema lockstep source (BD-RISK-7). | `client-engineering` + `backend-data-engineering` | PLANNED | `docs/engineering/backend-data-implementation.md` §4 | Foundation increment |
| WORK-013 | Sync-protocol conformance suite against real Supabase before any table is exposed: concurrent writers; forced clock skew; kill-mid-push replay; same-timestamp page-boundary; insert/update/tombstone `version` checks; `operation_id` dedupe; **in-flight-successor acknowledgement** (edit during a dispatched op → `O1` terminates → assert `O2` re-based to returned `version`, row still `dirty`, `O1` payload not applied); **late-transaction-commit reconciliation** (Tx A begins before the cursor, stays uncommitted while a newer row is pulled and the cursor advances, commits after; assert the next full reconciliation recovers A's row); **lost transport response with successor** (`O1` dispatched → concurrent edit creates `O2` → `O1`'s response is lost → assert `O1` stays `dispatched` and retryable with the same `operation_id`, no `UNIQUE (entity, entity_id) WHERE state='pending'` violation, retry returns `duplicate`, `O2` is re-based, `O2` subsequently applies); **completed-session conflict is parked** (assert no auto-re-issued pending mutation, `O2` dropped, `sync_conflicts` row present). | `software-architecture` + `backend-data-engineering` + `quality-engineering` | PLANNED | `system-architecture.md` §8.3–8.4, §10.2–10.5; `docs/architecture/adrs/ADR-0003-sync-engine.md` | Foundation increment (SPEC §18 Phase 0); depends on DEP-1 |

Statuses: `PLANNED`, `IN PROGRESS`, `COMPLETE`, `DEFERRED`, `OUT OF SCOPE`, `BLOCKED`.

## Artifact registry

| Artifact | Owner | Status | Consumers | Last validated |
|---|---|---|---|---|
| `docs/product/product-strategy.md` | `product-strategy` | APPROVED | All downstream phases | 2026-09-01 |
| `docs/product/ux-product-design.md` | `evidence-based-ui-ux` | APPROVED | Visual UI, architecture, client, quality | 2026-09-02 |
| `docs/design/visual-ui-design.md` | `visual-ui-design` | APPROVED | Architecture, client, quality | 2026-09-02 |
| `docs/design/renders/weight-visual-system-v1.html` | `visual-ui-design` | SUPPORTING (render, v2 content) | Client, quality (visual baseline) | 2026-09-02 |
| `docs/architecture/system-architecture.md` | `software-architecture` | APPROVED (v4) | Engineering, security, platform, quality, operations | 2026-09-02 |
| `docs/architecture/adrs/` | `software-architecture` | APPROVED (ADR-0001…0009 Accepted) | Engineering, security, platform, quality, operations | 2026-09-02 |
| `docs/engineering/client-implementation.md` | `client-engineering` | MISSING | Quality, operations, orchestrator | — |
| `docs/engineering/backend-data-implementation.md` | `backend-data-engineering` | APPROVED WITH CONDITIONS (contract; BD-C1 execution gate in force) | Security, quality, operations, orchestrator | 2026-09-02 |
| `supabase/` (migrations, functions, tests, seed) | `backend-data-engineering` (→ RLS/functions to `security-identity`) | APPROVED WITH CONDITIONS — **LOCALLY EXECUTED & GREEN 2026-09-02** (WORK-022): `db reset` ×2, `db lint` clean, `supabase test db` PASS 68/68 on Postgres 15.8. Hosted DEP-1 execution + CI gate still required. | Security, platform, quality | 2026-09-02 |
| `docs/security/security-identity.md` | `security-identity` | AWAITING APPROVAL | Platform, quality, operations, orchestrator | 2026-09-02 |
| `supabase/migrations/20260902090006_security_hardening.sql` | `security-identity` | AWAITING APPROVAL — WORK-022 fixes (F-2/F-5/F-8/F-11) applied & locally green; hosted re-verify required | Platform, quality, orchestrator | 2026-09-02 |
| `supabase/migrations/20260902090003_recompute.sql` | `backend-data-engineering` | AWAITING APPROVAL — WORK-022 fixes (F-7/F-9) applied & locally green | Security, platform, quality | 2026-09-02 |
| `supabase/tests/01…04_*.sql` (pgTAP) | `security-identity` + `backend-data-engineering` | AWAITING APPROVAL — WORK-022: F-1/F-3/F-4/F-6/F-10/F-12 + full-row payloads + negative test; **PASS 68/68** locally (plans 19/17/8/24) | Quality | 2026-09-02 |
| `docs/platform/platform-release.md` | `platform-release` | AWAITING APPROVAL (PASS WITH CONDITIONS) | Quality, operations, orchestrator | 2026-09-02 |
| `docs/platform/evidence/` | `platform-release` | SUPPORTING (executed command logs: db reset / db lint / test db, versions) | Quality, orchestrator | 2026-09-02 |
| `.github/workflows/db-verify.yml` | `platform-release` | AWAITING APPROVAL (DB verification merge gate; currently RED — F-2/F-5) | Quality, client-engineering | 2026-09-02 |
| `supabase/functions/delete-account/deno.json` + `deno.lock` | `platform-release` | AWAITING APPROVAL (SEC-RESID-2 dependency pin) | Security, quality | 2026-09-02 |
| `docs/quality/release-quality.md` | `quality-engineering` | MISSING | Operations, orchestrator | — |
| `docs/operations/production-operations.md` | `production-operations` | MISSING | Orchestrator and maintainers | — |
| `docs/implementation/verification-report.md` | `implementation-orchestrator` | MISSING | Human project owner | — |

## Human review log

| Phase | Submitted result | Decision | Conditions/waivers | Reviewer | Date |
|---|---|---|---|---|---|
| `product-strategy` | PASS WITH CONDITIONS | APPROVED | C-1: SM-1…SM-7 targets are unresearched assumptions; UX to establish baselines and may revise. C-2: OQ-1…OQ-10 tracked; each resolved before its affected phase locks. C-3: guest mode / seed catalog / deletion behavior / retention decision-gated on OQ-3/4/10/9. C-4: DEP-1…DEP-5 unprovisioned; Aeonik license (DEP-2) and exercise-catalog license (DEP-3) are hard prerequisites for their features. Design direction added: monochromatic + neumorphic, Aeonik typeface, reduce friction for logging/planning/viewing. | Human project owner | 2026-09-01 |
| `evidence-based-ui-ux` | PASS WITH CONDITIONS | APPROVED | UX-C1: artifact is description-based; reach/keyboard/interruption inferred — §14 study (incl. on-device gym session) runs before SM-* become gates; inherits C-1. UX-C2: task priorities and SM realism are assumptions, to reconcile with study. UX-C3: UX-DEC-07 (no per-set confirm) carries UX-RISK-2 with a defined reversal path. UX-C4: UX-OQ-1…6 open; UX-OQ-4 (onboarding depth) and UX-OQ-5 (dark mode at launch) are Product calls. Color direction added by human: "white sand" ground + "Persian blue" tonal scale — refines the strict-greyscale reading of CON-7. | Human project owner | 2026-09-02 |
| `visual-ui-design` | PASS WITH CONDITIONS | APPROVED | VIS-C1: validated as a browser prototype, not on-device RN — Dynamic Type reflow, VoiceOver/TalkBack order, RTL, real neumorphic rendering, measured contrast are verification targets for client/quality (WORK-007). VIS-C2: type assumes Aeonik; documented fallback stack ships until DEP-2 (files + licence); system is typeface-independent. VIS-C3: full light+dark tokens delivered, only 2 screens rendered dark — a launch dark mode needs dark renders + QA for all surfaces (Product call, OQ-8). VIS-C4: VIS-OQ-3 (icon family), VIS-OQ-4 (amber tier), VIS-OQ-5 (on-device neumorphic depth) open; none block architecture. VIS-C5: "white sand / Persian blue" read as single-hue (full blue scale), not literal greyscale+accent. | Human project owner | 2026-09-02 |
| `software-architecture` | PASS WITH CONDITIONS (v1) | REVISION REQUESTED | Sync correctness must be resolved before approval: (1) composite `(updated_at, id)` pull cursor with lexicographic continuation; (2) real optimistic concurrency — server integer `version`, insert-if-absent / update-if-`base_version` / atomic increment / conflict-not-overwrite / same for tombstones; (3) client-generated `operation_id` per outbox mutation + server dedupe, and specify replay model (coalesced vs sequential); (4) remove client-clock conflict ordering — winner decided by server `version`, server `updated_at` for remote ordering only, local timestamps for local ordering only; (5) correct the hot-write acknowledgement (no same-frame/≤16 ms commit claim; ≤100 ms perceived persistence; durable only after commit; failure → "Not saved — retrying" + Finish blocked). Minor: duplicated-heading check, `ret+ry` typo, "Phase 0" vs lifecycle phase terminology. | Human project owner | 2026-09-02 |
| `software-architecture` | PASS WITH CONDITIONS (v2) | REVISION REQUESTED | Two remaining synchronization races: (1) **in-flight successor acknowledgement incomplete** — §10.2 must not clear `dirty` / delete the entry / apply the returned payload when a `pending` successor exists; specify the successor-aware ack transaction and reconcile the latest local payload (incl. successor) on an in-flight conflict; relax "exactly one pending entry" to "≥1 outstanding mutation". (2) **composite cursor does not cover late transaction commits** — a row written at `t1` but committed after a `t2 > t1` row is pulled lands behind the cursor forever; replace the absolute "cannot skip" claim with periodic full `(id, version)` reconciliation (recommended for MVP) or a commit-safe server change-feed; add a WORK-013 late-commit recovery test. | Human project owner | 2026-09-02 |
| `software-architecture` | PASS WITH CONDITIONS (v3) | REVISION REQUESTED | Correct transport-failure state handling: §10.2 returned `O1` to `pending` on transport/5xx while a concurrent-edit successor `O2` was already `pending` → two `pending` rows → `UNIQUE (entity, entity_id) WHERE state='pending'` violation. Required: a dispatched op is immutable until a terminal protocol result; rename/define `in_flight` as a durable `dispatched`/`retry_wait` state; on transport/5xx keep `O1` dispatched (preserve `operation_id`/payload/`base_version`, update attempts/retry time), keep `O2` pending behind it; retry dispatched before pending successors; recover+retry dispatched with the same `operation_id` after process death; only `applied`/`duplicate`/`conflict` terminate `O1`; return to `pending` only if provably never dispatched and no successor. Add the WORK-013 lost-response-with-successor case. Also clarify a completed-session conflict is parked in `sync_conflicts` for explicit user choice, not auto-re-issued as a pending mutation. | Human project owner | 2026-09-02 |
| `software-architecture` | PASS WITH CONDITIONS (v4) | APPROVED | AR-C1: two `recompute` implementations (TS device + SQL/Edge server) kept convergent by one spec + shared golden vectors (WORK-012) + server-wins-on-pull — managed risk (AR-RISK-2). AR-C2: conflict policy is server-`version` optimistic concurrency + durable `dispatched` state + parked completed-session conflicts; correct for any device count, only the multi-device review UX is thin (AR-RISK-1, OQ-7). AR-C3: `expo-sqlite` transaction/WAL guarantees + hand-rolled reactive query layer must be verified against the locked Expo SDK at the start of the Foundation increment (WORK-010); negative result pulls the dev-build earlier. AR-C4: boundary-lint CI gate (WORK-011) + sync-protocol conformance suite (WORK-013) are hard Foundation-increment prerequisites. AR-C5: AR-OQ-1…6 open, none block backend/security. `backend-data-engineering` unlocked next by explicit human choice (ahead of `client-engineering`). | Human project owner | 2026-09-02 |
| `security-identity` | PASS WITH CONDITIONS | _pending_ | Threat model + RLS-as-boundary; child→parent ownership via composite FKs + soft-ref triggers (SEC-F-1 High, fixed); definer recompute + client-DML revoke resolves BD-OQ-2 (SEC-F-2); deletion-cascade FKs + non-PII `deletion_receipts` for OQ-10 hard cascade (SEC-F-3/6); hardened `sync_apply` — no injection, no id-oracle (SEC-F-4/5); guarded global-seed model (SEC-F-7); corrected weekly bucketing per human gate 6 (SEC-F-9, routed to backend to validate). SEC-DEC-01…05, WORK-016/018–021, SEC-OQ-1, SEC-RESID-1/2, SEC-RISK-1/2. **SEC-C1: nothing executed (no DEP-1) — `client-engineering` stays LOCKED until suites 01–04 pass on a provisioned project.** SEC-C2: hosted auth config + secrets + dependency pin → platform-release. SEC-C3: nonce reauth + retention policy + pen test before beta. SEC-C4: SEC-F-9 boundary note. SEC-C5: `exercises` dual-tenancy deviation accepted. Awaiting human decision (recommended next: `platform-release` to discharge the DEP-1 gate). | — | — |
| `security-identity` (disposition) | PASS WITH CONDITIONS | HELD — NOT APPROVED | Human 2026-09-02: phase 7 is authored + reviewed but has **0 executed tests**; the verification gate is unsatisfied. It stays `Pass with Conditions` / unapproved until DEP-1 is provisioned and the security verification suite executes successfully. `platform-release` proceeds next to provision DEP-1. | Human project owner | 2026-09-02 |
| Governance / lifecycle (cross-cutting) | — | DIRECTION SET | Human 2026-09-02: (1) **Notion Shared Project Memory becomes the sole canonical governance source**; existing accepted governance is migrated **once** from `development-roadmap.md` + `docs/` (Option B — no dual/hybrid). Scope: accepted lifecycle Decisions (preserve IDs), ADR-0001…0009, the requirement set, lifecycle phases/gates → Milestones, still-relevant unresolved risks/open questions, and historical reviews only where they materially establish approve/block rationale. Exclude implementation noise / obsolete notes / every historical task. Sequence: ChatGPT writes the four governing DEC records into Notion, then issues Claude Code a controlled governance-migration prompt. (2) **`platform-release` is the next lifecycle phase.** (3) **Phase 7 unapproved until DEP-1 execution evidence exists.** (4) **300 s re-auth heuristic is development-only; server-verifiable re-auth required before beta.** OPEN: SEC-OQ-1 retention periods pending infrastructure-capability + data-class review. | Human project owner | 2026-09-02 |
| `backend-data-engineering` | PASS WITH CONDITIONS | APPROVED WITH CONDITIONS | Accepted BD-C1…BD-C5. Decision: proceed to `security-identity`. **OQ-10 resolved: hard cascade deletion for MVP; retain only a non-PII deletion receipt outside the deleted user graph.** BD-C5 deviations accepted. Only `backend-data-implementation.md` inspected directly (the `supabase/` files were not attached) — approval accepts the documented implementation contract; BD-C1 and later code/runtime verification gates remain fully in force. **8 gates for phase 7:** (1) `client-engineering` LOCKED until migrations + lint + pgTAP execute successfully on DEP-1; (2) enforce child→parent ownership integrity — a child's client-supplied `user_id` must never reference another user's parent row (composite FKs or trusted triggers required); (3) resolve BD-OQ-2 with narrowly scoped recompute functions, safe ownership, fixed `search_path`, explicit execute grants under FORCE RLS; (4) clarify the global-seed exercise ownership model vs. the standard non-null `user_id` contract; (5) finalise + adversarially test every RLS policy incl. `sync_apply`, seed visibility, derived tables, cross-account parent references; (6) resolve BD-OQ-1 before Data & Progress — weekly aggregates use the user's configured `week_start` and the session-local calendar date, not UTC `date_trunc`; (7) OQ-10 hard cascade + non-PII receipt; (8) exercise catalogue non-shippable until OQ-4 licensing resolved. | Human project owner | 2026-09-02 |
| `platform-release` | PASS WITH CONDITIONS | _pending_ | Executed 2026-09-02 (`docs/platform/platform-release.md`, `docs/platform/evidence/`). Local Supabase provisioned on Postgres 15.8; `supabase db reset` ✅ (repeatable fresh-create of migrations 0001–0006 + seed); `supabase db lint --level error --fail-on error` ✅ (1 warning-level finding F-7); `supabase test db` ❌ FAIL. Real execution surfaced **F-5** (High — migration 0006 `trg_recompute_from_*` call `_week_start_for(date, coalesce(week_start,1))` → `_week_start_for(date, integer) does not exist`; every completed-session `performed_sets` write throws) and **F-2** (Medium — `sync_apply` INSERT leaks a raw NOT NULL exception on partial payloads instead of `{status:"rejected"}`), plus test-quality findings F-1 (fixed: `perform`→`select` at top-level SQL scope, all 4 suites), F-3/F-4/F-6 (suite 01 assertion matchers + plan count), F-7 (`v_keep uuid[]` cast). Where RLS assertions ran (01: 15, 04: 6) they PASSED. **SEC-RESID-2 resolved** — `supabase/functions/delete-account/deno.json` + `deno.lock`, `@supabase/supabase-js` pinned to `2.112.4`. `config.toml` expanded (env separation, hosted-auth hardening intent), `.env.example` client-safe/server-only contract, `db-verify` CI gate added (currently RED by design). **Conditions:** C-1 no git repo (CI cannot run; branch protection blocked) — human `git init` + GitHub repo; C-2 hosted DEP-1 (dev + prod Supabase projects) is a human decision (org + authorization) — all hosted provisioning / hosted auth hardening / secret provisioning / Edge Function deploy / PITR deferred; C-3 `supabase test db` NOT green → **phases 6 & 7 remain NOT verified** (upholds DEC-3); F-5 + F-2 must be fixed by `security-identity`/`backend-data-engineering` (WORK-022) and all suites green before `client-engineering` unlocks; C-4 F-1 pgTAP fix needs owner ratification; C-5 Edge Function pinned to 2.112.4 (not latest) pending supply-chain min-age. `client-engineering` stays `LOCKED`. Recommended next human action: authorize DEP-1 + `git init`, then task `security-identity`/`backend-data-engineering` with WORK-022. | — | — |

| WORK-022 (lifecycle recovery) | PASS | _pending_ | Human authorised narrowly-scoped recovery across `security-identity` + `backend-data-engineering` after phase-8 execution found 12 defects (F-1…F-12; F-5 & F-9 High — both would break the recompute path at runtime). All fixed **in place** (migrations unshipped) per the human's approved resolutions: F-5 → `1::smallint` at 4 boundaries (signature NOT widened); F-2 → `not_null_violation` caught → structured `{status:"rejected"}` (defaults not merged; happy-path tests send full rows; explicit partial-payload negative test added); F-1 ratified (12 statements, `perform` in `$$` bodies preserved); F-3/F-4 → 0-rows + protected-row-unchanged assertions; F-6 → plans corrected to 19/17/8/24 (also fixed pre-existing suite-02/04 miscounts); F-7 → `array[]::uuid[]`; F-8 → dedupe returns `duplicate` per the RPC contract; F-9 → `extensions.`-qualified uuid-ossp calls (pinned `search_path` unchanged, SEC-REQ-AZ-07 upheld); F-11 → `revoke sync_apply from anon`; F-12 → `::numeric` casts; F-10 → test corrected to the SEC-DEC-05 boundary + **ISS-27** opened (anon read of the seed catalogue — decide before beta). CLAUDE.md verified to already carry the DEC-1 authority correction (attached review copy was stale). Verification: `supabase db reset` ×2 ✅, `supabase db lint` clean at warning + error ✅, `supabase test db` **PASS 68/68** ✅, runtime probes ✅ (completed-session recompute, `week_start` 0–6, `sync_apply` full/replay/partial/anon). Evidence `docs/platform/evidence/05-08`. **Not done:** hosted re-run on a provisioned project, CI gate green, formal client-TS ↔ server golden-vector cross-run. Migrations confirmed NOT deployed to any hosted/released env. | Claude Code (`security-identity` + `backend-data-engineering`) | 2026-09-02 |

## Deferred and out-of-scope work

| Item | Classification | Reason | Revisit trigger | Owner |
|---|---|---|---|---|
| Provider / social login | DEFERRED | Post-MVP per `SPEC.md` §10.3 | After MVP release | Product, Security |
| Alternative e1RM formulas (user-selectable) | DEFERRED | Epley-only for MVP; formula id/version stored to allow later change | OQ-6 resolution | Product, Backend |
| Richer true-simultaneous multi-device conflict UX | DEFERRED | MVP uses last-accepted-write-wins + conflict telemetry | OQ-7 resolution | UX, Architecture |
| Exercise media / Supabase Storage | DEFERRED | Not needed for MVP | Exercise media added to scope | Backend |
| Coaching, teams, social, nutrition, wearables, AI programming, video analysis, marketplace, web/desktop authoring, advanced periodization | OUT OF SCOPE | `CLAUDE.md` product boundaries; DEC-001 | Explicit roadmap scope change + human approval | Product |
| Apple Health / Health Connect / watch apps / live activities | OUT OF SCOPE | `SPEC.md` §2.3 | Post-MVP roadmap change | Product, Platform |
