# Expo SDK 54 + npm: the RN 0.81 / React 19.1 peer graph (via @expo/cli and
# expo-router's @expo/metro-runtime) does not fully satisfy npm's strict peer
# resolver. `npx expo install` resolves it; plain `npm install` needs this.
legacy-peer-deps=true
