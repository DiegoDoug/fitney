# Expo / RN
node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.DS_Store

# Metro / build
.metro-health-check*
*.tsbuildinfo

# local env (never commit real values — .env.example documents the contract)
.env
.env.local
.env*.local

# native (generated by prebuild; not committed during the Expo Go phase)
/ios
/android

# test / coverage
coverage/
